[build]
  command = "npm run docs:build"
  publish = "docs/.vitepress/dist"  # ← This is the correct path for VitePress

[build.environment]
  NODE_VERSION = "20"  # Backup for .nvmrc

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
